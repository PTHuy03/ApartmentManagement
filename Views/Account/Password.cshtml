@model ApartmentManagement.Models.User

@{
    Layout = "_Layout";  // Nếu bạn sử dụng layout chung
}

<div class="container mt-5">
    <!-- Cover section -->
    <div class="bg-dark text-white text-center py-5 position-relative" style="background-image: url('../img/cover.jpg'); background-size: cover; background-position: center; height: 250px;">
        <div class="position-absolute start-0 bottom-0 ms-4 mb-4 d-flex align-items-center">
            <img src="@Model.Avatar" alt="Avatar" class="rounded-circle border border-white" style="width: 100px; height: 100px;">
            <h3 class="ms-3 fw-bold">@Model.FullName</h3>
        </div>
    </div>

    <!-- Main section -->
    <div class="row mt-4">
        <!-- Sidebar -->

        <div class="col-md-3 mb-3">
            <div class="card">
                <div class="card-header fw-bold">
                    CÀI ĐẶT
                </div>
                <div class="card-body list-group ps-3">
                    <a class="list-group-item list-group-item-action" asp-controller="Account" asp-action="Profile" asp-route-email="@Model.Email">
                        <i class="bi bi-person-lines-fill me-2"></i>Hồ sơ
                    </a>
                    <a class="list-group-item list-group-item-action" asp-controller="Account" asp-action="Avatar" asp-route-email="@Model.Email">
                        <i class="bi bi-camera-fill me-2"></i>Ảnh đại diện
                    </a>
                    <a class="list-group-item list-group-item-action active" asp-controller="Account" asp-action="Password" asp-route-email="@Model.Email">
                        <i class="bi bi-key-fill me-2"></i>Mật khẩu
                    </a>
                </div>
            </div>
        </div>

        <!-- Profile content -->
        @if (!ViewData.ModelState.IsValid)
        {
            <div class="alert alert-danger">
                @Html.ValidationSummary()
            </div>
        }
        <div class="col-md-9">
            <div class="card">
                <div class="card-header fw-bold">
                    THAY ĐỔI MẬT KHẨU CỦA BẠN
                </div>
                <div class="row items-push">
                    <div class="col-lg-3 m-3">
                        <p>
                            Giữ an toàn cho tài khoản bằng cách thay đổi mật khẩu thường xuyên.
                        </p>
                    </div>
                    <div class="card-body col-lg-7 me-5">
                        <form method="post">
                            <div class="mb-3">
                                <label class="form-label">Mật khẩu hiện tại</label>
                                <input class="form-control" type="password" id="oldPassword" name="oldPassword" required />
                                @if (ViewData.ModelState["OldPassword"] != null)
                                {
                                    <div class="text-danger mt-1">
                                        @ViewData.ModelState["OldPassword"].Errors[0].ErrorMessage
                                    </div>
                                }
                            </div>
                            <div class="mb-3">
                                <label class="form-label">Mật khẩu mới</label>
                                <input class="form-control" type="password" id="newPassword" name="newPassword" required />
                                @if (ViewData.ModelState["NewPassword"] != null)
                                {
                                    <div class="text-danger mt-1">
                                        @ViewData.ModelState["NewPassword"].Errors[0].ErrorMessage
                                    </div>
                                }
                            </div>
                            <div class="mb-3">
                                <label class="form-label">Nhập lại mật khẩu mới</label>
                                <input class="form-control" type="password" id="confirmNewPassword" name="confirmNewPassword" required />
                                @if (ViewData.ModelState["ConfirmNewPassword"] != null)
                                {
                                    <div class="text-danger mt-1">
                                        @ViewData.ModelState["ConfirmNewPassword"].Errors[0].ErrorMessage
                                    </div>
                                }
                            </div>
                            <button type="submit" class="btn btn-primary">Đổi mật khẩu</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}